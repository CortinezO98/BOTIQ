fastapi==0.111.0
uvicorn[standard]==0.30.0
python-multipart==0.0.9
sqlalchemy==2.0.30
alembic==1.13.1
psycopg2-binary==2.9.9
asyncpg==0.29.0
pydantic==2.7.1
pydantic-settings==2.2.1
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
bcrypt==4.0.1
rapidfuzz==3.9.6
google-cloud-aiplatform==1.52.0
google-cloud-vision==3.7.2
google-cloud-documentai==2.24.0
google-cloud-storage==2.17.0
google-auth==2.29.0
google-auth-oauthlib==1.2.0
google-api-python-client==2.129.0
# Fijado a la serie 1.x: chromadb 0.5.0 usa np.float_, eliminado en NumPy 2.0
numpy==1.26.4
chromadb==0.5.0
# Actualizado 10.3.0 -> 12.3.0 (pip-audit: varias PYSEC, no usado directo en
# el código -- llega vía qrcode[pil] para el QR de MFA -- verificado generando
# un QR real con pyotp antes de subir la versión.
Pillow==12.3.0
httpx==0.27.0
# Actualizado 3.9.5 -> 3.14.1 (pip-audit: ~25 PYSEC). No se importa directo
# en ningún archivo de la app -- dependencia transitiva. pip check confirma
# que el resto de requirements.txt sigue resolviendo sin conflictos.
aiohttp==3.14.1
# Actualizado 1.0.1 -> 1.2.2 (pip-audit: PYSEC-2026-2270). Sin cambios de API.
python-dotenv==1.2.2
tenacity==8.3.0
gunicorn==22.0.0
# Actualizado 8.2.0 -> 9.0.3 (pip-audit: PYSEC-2026-1845). Verificado contra
# conftest.py (fixture event_loop personalizado) antes de subir: pasa sin
# warnings ni errores nuevos con pytest-asyncio 1.4.0.
pytest==9.0.3
# Actualizado 0.23.7 -> 1.4.0: es la versión mínima compatible con pytest 9
# (pip falla la resolución de dependencias con 0.23.7 + pytest 9).
pytest-asyncio==1.4.0
openpyxl==3.1.5
# MFA (TOTP) para rol admin
pyotp==2.9.0
qrcode[pil]==7.4.2
# Actualizado 4.3.1 -> 6.13.3 (pip-audit: ~20 PYSEC + GHSA-jm82-fx9c-mx94).
# Usado en support_rag/service.py (PdfReader, .pages, .extract_text()) --
# verificado con un PDF real antes de subir la versión, misma API, mismo
# resultado.
pypdf==6.13.3
# Rate limiting en endpoints críticos (login, register, chat)
slowapi==0.1.9
limits==3.13.0

pytest-cov==7.1.0
pytest-env==1.1.5